name: Build and Push Docker Container

on:
  push:
    branches:
      - 'develop-*'
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      tag:
        required: false
        description: 'Tag to build (e.g., v1.2.3)'


jobs:
  build-jmx-exporter-container:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.inputs.tag }}

      - name: Read version from file and set output
        id: jmx_version
        run: |
          VERSION=$(cat jmx_exporter_version.txt)
          echo "VERSION=$VERSION"
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v4

      - name: Login to GHCR
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4
              
      - name: Extract metadata (tags, labels) for Docker
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: |
            ghcr.io/${{ github.repository }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}},prefix=int-,match=v(\d.\d.\d)$,event=tag
            type=semver,pattern={{version}},value=${{ github.event.inputs.tag }},enable={{is_default_branch}}
            type=semver,pattern={{major}}.{{minor}},value=${{ github.event.inputs.tag }},enable={{is_default_branch}}
            type=semver,pattern={{major}},value=${{ github.event.inputs.tag }},enable={{is_default_branch}}
            type=raw,value=latest,event=branch
            type=raw,value=jmx-exporter-${{ steps.jmx_version.outputs.version }},event=branch,enable={{is_default_branch}}
            type=raw,value=dev,enable=${{ startsWith(github.ref_name, 'develop-') }}

      -   name: Build and push Docker images
          id: push
          uses: docker/build-push-action@v7
          with:
            file: ./Dockerfile
            push: true
            tags: ${{ steps.meta.outputs.tags }}
            labels: ${{ steps.meta.outputs.labels }}
            build-args: |
              JMX_EXPORTER_VERSION=${{ steps.jmx_version.outputs.version }}
            platforms: linux/amd64,linux/arm/v7,linux/arm64
            outputs: type=image,name=target,annotation-index.org.opencontainers.image.description=Prometheus jmx-exporter https://prometheus.github.io/jmx_exporter/${{ steps.jmx_version.outputs.version }}/,annotation-index.org.opencontainers.image.version=${{ steps.jmx_version.outputs.version }}
